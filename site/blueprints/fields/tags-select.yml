type: entries
label: Schlagwörter auswählen
max: 4
field:
  type: select
  required: true
  options:
    type: query
    query: site.tags.toStructure
    text: "{{ item.name }}"
    value: "{{ item.customUuid }}"
