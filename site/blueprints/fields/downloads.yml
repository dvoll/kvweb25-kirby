label: Downloads
type: files
layout: cardlets
text: "{{ file.title.isNotEmpty ? file.title : file.filename }}"
info: "{{ file.title.isNotEmpty ? file.filename : '' }}"
uploads: download
query: page.files.filterBy("template", "in", ["download"])
