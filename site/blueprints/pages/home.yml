# Each page blueprint must have a title, the title may be different from the file name
title: Home

extends: layouts/default

# Each page can have an icon that is shown in page listings when no preview image is available.
image: 
  icon: home
  query: false

# Page options allow you to control how users can interact with the page.
# Here we prevent changing the page status (the home page cannot be renamed or deleted by default)
# More about page options: https://getkirby.com/docs/reference/panel/blueprints/page#options
options:
  changeStatus: false


tabs:
  content:
    columns:
      # sidebar: false
      sidebar:
        fields:
          children: false
          - label: Info
            type: info
            theme: passive
            text: |
              Erst muss ein Abschnitt hinzugefügt werden, in dem dann Blöcke erstellt werden können. 
              Zwischen Abschnitten sind die Abstände größer. 
              
              Siehe dazu: (link: https://cvjm-kreisverband.de text: Info zu Layouts ⮺ target: _blank)
