name: Galerie (Swiper)
icon: dashboard
preview: gallery
fields:
  images:
    extends: fields/image
    label: field.blocks.gallery.images.label
    multiple: true
    layout: cards
    size: small
    image:
      ratio: 1/1
  info:
    label: Bildunterschrift
    type: info
    template: info
    text: Am Bild (Aufruf durch klick auf das Vorschaubild) kann jeweils eine Bildunterschrift gepflegt werden, die in der Galerie angezeigt wird.
